#include <stdio.h>
/*
 * main - Affiche le nombre d'argument passé en ligne de commande
 * @argc : Nombre d'argument
 * @argv : Tableau d'argument
 * Return: 0 si succès
 */
int main(int argc, char *argv[])
{
	(void)argv; /*éviter le warning car argv non utlisé*/
	printf("%d\n", argc - 1);/*Afficher le nombre d'argument réels*/
	return (0);
}

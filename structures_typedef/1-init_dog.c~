#include "main.h"
#include "dog.h"

void init_dog(struct dog *d, char *name, float age, char *owner)
{
	/*si d différent de null alors tu exécutes*/
	if (d != NULL)
	{
		d ->name = name;
		d ->age = age;
		d ->owner = owner;
	}
}

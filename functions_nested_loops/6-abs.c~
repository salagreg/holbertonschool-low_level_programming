#include "main.h"
#include <stdio.h>
/**
* _abs - calculer la valeur absolue
* @r: valeur ajoutée
* Return: retourne r
*/
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return(r);
	}
}

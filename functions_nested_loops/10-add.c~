#include "main.h"
#include <stdio.h>
/**
* add - additionner deux valeurs
* @i @j : valeur que je dois additionner
* Return: n , je retourne le résultat de i + j
*/
int add(int i, int j) /* valeur que je dois additionner */
{
	int n = i + j;

	return (n);
}

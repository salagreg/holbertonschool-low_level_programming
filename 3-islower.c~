#include "main.h"
#include <stdio.h>
/**
* _islower - Affiche l'alphabet en minuscule
* int c : test
* Return: 1
*/
int _islower(int c) /* test */ 
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

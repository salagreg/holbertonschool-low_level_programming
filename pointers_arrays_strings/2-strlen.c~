#include "main.h"
#include <string.h>
/**
* _strlen - renvoie la longueur de chaines
* @s: contient les valeurs de longueur
* Return: i 
*/
int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++;
	}
	return (i);
}

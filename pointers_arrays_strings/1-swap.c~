#include "main.h"
/**
* swap_int - permute les valeurs
* @a : contient une valeur
* @b : contient une valeur
* Return : neant
*/
void swap_int(int *a, int *b)
{
	int temp = 0;

	temp = *a;
        *a = *b;
        *b = temp;
}
